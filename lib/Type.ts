/**
 * An enum that represents the various types of a JSON object.
 * Undefined and null are treated equally.
 */
export enum Type {
    string,
    number,
    boolean,
    dictionary,
    array,
    null,
}
